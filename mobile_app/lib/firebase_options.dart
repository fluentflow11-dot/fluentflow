// Stub to keep builds green before real Firebase config is added.
// Replace this file with the generated one from FlutterFire CLI.
// Command after creating Firebase project:
//   flutterfire configure --project=<your-project-id> --out=lib/firebase_options.dart

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
	static FirebaseOptions get currentPlatform {
		throw StateError(
			'Firebase not configured. Run flutterfire configure to generate lib/firebase_options.dart',
		);
	}
}


