rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary dev rules: open until 2026-01-01. Tighten before release.
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 1, 1);
    }
  }
}


